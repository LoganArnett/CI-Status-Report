export interface StatusDataObject {
  buildNumber: string;
  commitSha: string;
  commitUrl: string;
  committerName: string;
  branchName: string;
  targetUrl: string;
  owner: string;
  repo: string;
}
